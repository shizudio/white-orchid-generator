{
  "name": "white-orchid-mcp",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "MCP server that drives the real White Orchid Content Studio headlessly (Playwright) — generation, refinement, and export all run through the app's own client pipeline. Two entries: index.mjs (local stdio) and http.mjs (remote Streamable HTTP).",
  "bin": { "white-orchid-mcp": "index.mjs" },
  "scripts": {
    "start": "node http.mjs",
    "stdio": "node index.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "playwright": "1.61.1"
  }
}
