node_modules
dist
.next
.env
.env.local
.env*.local
!.env.example
.DS_Store
.vercel
*.psd
*.bak
.gstack/

# generated preview exports (local only)
generated/

# Resident tester isolated production build
.next-resident/

# Isolated dev-server dist (wo-3210-iso launch config)
.next-3210/

# isolated build dirs + codedb tool snapshot
.next-*/
codedb.snapshot

# MCP server output artifacts (wo_export_post files, never committed)
mcp-server/out/

# MCP server persistent browser profile (device-local sessions)
mcp-server/.profile/
