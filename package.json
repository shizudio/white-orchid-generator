{
  "name": "white-orchid",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test:unit": "node --test scripts/tests/*.test.mjs",
    "test:resident": "WO_DIST_DIR=.next-resident NEXT_PUBLIC_WO_TEST_HOOKS=1 next build && node scripts/resident-tester/run.js",
    "test:resident:nightly": "WO_DIST_DIR=.next-resident NEXT_PUBLIC_WO_TEST_HOOKS=1 next build && WO_NIGHTLY=1 node scripts/resident-tester/run.js --nightly"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
